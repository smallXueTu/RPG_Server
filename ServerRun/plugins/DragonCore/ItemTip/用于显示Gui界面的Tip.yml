# 这个文件用来将所有  没有设置Tip样式  的物品设置一个通用样式
# 为了保证图片不会因自动拉伸而显得丑陋无比
# 我们使用九宫格绘图法
# 就是将图片分为9个区域绘制
# 1 2 3
# 4 5 6
# 7 8 9
# 其中，1379是固定大小，不会自动变化
# 46 会自动上下拉伸
# 28 会自动左右拉伸
# 5  作为文字背景，会全部拉伸



setting:
  # Shift 选项
  # 0 为按与不按都 显示模板
  # 1 为不按       显示模板
  # 2 为按下       显示模板
  shifttype: 0
  # 这个match匹配的是自定义Gui的tip
  match: "DragonCore_ToolTip_*"

  #width和height这两个选项的作用是：用于防止UI画在鼠标右侧时，超出屏幕宽度/高度，这时候可以自动挪到其他位置
  # 如果设置此选项(默认为0)，则当 鼠标x+width > 屏幕宽度时，所有UI会自动向左平移 move 的距离
  width: "w+12"
  move: "w+12"
  # 如果设置此选项(默认为0)，则当 鼠标y+height> 屏幕高度时，所有UI会自动向上平移 直到y+height < 屏幕高度  的距离 
  height: "h"


# 变量列表
# x  鼠标X
# y  鼠标y
# w  物品原本显示的lore(包含原版完整显示的内容,比如名字,附魔等)，最长一条的长度
# h  物品原本显示的lore(包含原版完整显示的内容,比如名字,附魔等)，总共的高度
# iw 物品的lore(仅Lore和名字)，最长一条的长度
# ih 物品的lore(仅Lore和名字)，总共的高度
# sw 屏幕宽度(ScreenWidth)
# sh 屏幕高度(ScreenHeight)

#任意一个名称
背景:
  #类型: 画一个渐变颜色块
  type: "texture"
  #以哪个位置为起始点，比如这里写了"x+12"和"y-12"，就是以鼠标当前x+12，y-12开始绘制
  x: "x+7"
  y: "y-17"
  #要画的宽度和长度
  width : "w+9"
  height: "h+7"
  path:  "tips/背景.png"
左上角:
  #类型: 画一张贴图
  type: "texture"
  #以哪个位置为起始点
  x : "x+15-10"
  y : "y-9-10"
  #要画的宽度和长度
  width : "5"
  height: "5"
  #贴图的文件路径
  path:  "tips/左上角.png"
右上角:
  #类型: 画一张贴图
  type: "texture"
  #以哪个位置为起始点
  x : "x+12+w"
  y : "y-19"
  #要画的宽度和长度
  width : "5"
  height: "5"
  #贴图的文件路径
  path:  "tips/右上角.png"
左下角:
  #类型: 画一张贴图
  type: "texture"
  #以哪个位置为起始点
  x : "x+15-10"
  y : "y-11+h"
  #要画的宽度和长度
  width : "5"
  height: "5"
  #贴图的文件路径
  path:  "tips/左下角.png"
右下角:
  #类型: 画一张贴图
  type: "texture"
  #以哪个位置为起始点
  x : "x+12+w"
  y : "y-11+h"
  #要画的宽度和长度
  width : "5"
  height: "5"
  #贴图的文件路径
  path:  "tips/右下角.png"

左边:
  #类型: 画一张贴图
  type: "texture"
  #以哪个位置为起始点
  x : "x+5.5"
  y : "y-15"
  #要画的宽度和长度
  width : "4.5"
  height: "h+4"
  #贴图的文件路径
  path:  "tips/左边.png"
右边:
  #类型: 画一张贴图
  type: "texture"
  #以哪个位置为起始点
  x : "x+12.5+w"
  y : "y-15"
  #要画的宽度和长度
  width : "4"
  height: "h+4"
  #贴图的文件路径
  path:  "tips/右边.png"

上边:
  #类型: 画一张贴图
  type: "texture"
  #以哪个位置为起始点
  x : "x+10"
  y : "y-18.5"
  #要画的宽度和长度
  width : "w+1.8"
  height: "4.5"
  #贴图的文件路径
  path:  "tips/上边.png"
下边:
  #类型: 画一张贴图
  type: "texture"
  #以哪个位置为起始点
  x : "x+10"
  y : "y-11+h"
  #要画的宽度和长度
  width : "w+1.8"
  height: "4.5"
  #贴图的文件路径
  path:  "tips/下边.png"



#调整原版信息行的显示位置
#首行为名字，最后可能还含有物品攻击力之类的信息(比如钻石剑，附魔)
#没有配置的行不会显示出来，999,999的话会显示到看不见的位置
Lines:

  - "x+12,y-12"
  - "x+12,y-11+i*10"
  - "x+12,y-11+i*10"
  - "x+12,y-11+i*10"
  - "x+12,y-11+i*10"
  - "x+12,y-11+i*10"
  - "x+12,y-11+i*10"
  - "x+12,y-11+i*10"
  - "x+12,y-11+i*10"
  - "x+12,y-11+i*10"
  - "x+12,y-11+i*10"
  - "x+12,y-11+i*10"
  - "x+12,y-11+i*10"
  - "x+12,y-11+i*10"
  - "x+12,y-11+i*10"
  - "x+12,y-11+i*10"
  - "x+12,y-11+i*10"
  - "x+12,y-11+i*10"
  - "x+12,y-11+i*10"
  - "x+12,y-11+i*10"
  - "x+12,y-11+i*10"
  - "x+12,y-11+i*10"
  - "x+12,y-11+i*10"
  - "x+12,y-11+i*10"
  - "x+12,y-11+i*10"
  - "x+12,y-11+i*10"
  - "x+12,y-11+i*10"
  - "x+12,y-11+i*10"
